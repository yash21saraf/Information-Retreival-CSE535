The jtt.py is the preprocessing file and also used to hit the URL.

The preprocessing is converting the tweet into all 3 languages and saving it into json file. 


Then this JSON file has been used to hit the URL. 

output.txt ---- The file generated by the jtt.py as the output
qrel.txt ------ Manual relevance for the 15 queries is trainqueries 
testqueries.txt ---- Contains 5 queries used for testing 
trainqueries.txt --- Contains queries used to optimize MAP 
train_queries.json ---- Contains queries converted in all languages 
test_queries.json ---- Contains queries converted in all languages

